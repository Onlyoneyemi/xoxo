let myBook = {
    title: "1984",
    author: "George Orwell",
    pageCount: 326
}

console.log(`${myBook.title} by ${myBook.author}.`)

myBook.title= "Animal farm"

console.log(`${myBook.title} by ${myBook.author}.`)


// Challenge Area
let person = {
    name: "Bello",
    age: 24,
    location: "Akure"
}

console.log(`${person.name} is ${person.age} years old and stays in ${person.location}.`)

person.age = person.age + 1

console.log(`${person.name} is ${person.age} years old and stays in ${person.location}.`)